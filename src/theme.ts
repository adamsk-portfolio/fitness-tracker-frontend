import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: { mode: 'dark', primary: { main: '#10b981' } }, // emerald-500
  shape:   { borderRadius: 10 },
});

export default theme;
